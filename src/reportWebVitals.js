const reportWebVitals = (onPerfEntry) => {
  // Web Vitals collection disabled to avoid extra dependency.
  // You can re-enable by installing 'web-vitals' and restoring the dynamic import.
  return;
};

export default reportWebVitals;
